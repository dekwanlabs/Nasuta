{{ .RolePrompt }}

Return exactly one JSON object with only:
- "focus": "{{ .Focus }}"
- "summary": concise evidence-based summary
- "findings": array of {"claim": string, "entity_ids": [exact task entity IDs directly supported by this finding], "evidence_goal_ids": [required facet strings], "evidence": [{"kind": string, "reference": string, "summary": string, "evidence_id": optional canonical handle, "identity": optional canonical identity}], "confidence": number from 0 to 1}
- "gaps": array of strings
- "covered_evidence_goals": required facet strings supported by at least one concrete finding
- "unresolved_evidence_goals": every remaining required facet string

The workflow task directive supplies the exact required facet strings. Partition every required facet into exactly one of covered_evidence_goals or unresolved_evidence_goals. Every covered goal must appear in evidence_goal_ids on a finding backed by concrete task-contract seed material or tool-returned evidence. Tool results may include a `_nasuta_evidence_manifest`. Cite its `evidence_id` exactly in the matching evidence item; this is preferred over manually copying an identity. `identity` is only a canonical object with source_kind, target, and optional section/version/time_range. Never put a service UUID, serviceKey, document ID, or other plain string in `identity`; omit identity when you have an evidence_id or only a reference. Never invent, normalize, or broaden an evidence ID or identity. Set `entity_ids` to exact IDs from the task contract and split findings when one claim is not supported for the same entity set. Prefer fewer complete findings over an exhaustive response that risks truncating the JSON object. Put unsupported conclusions and unavailable evidence in gaps. Do not return markdown or text outside the JSON object.
