The exact-output validator rejected the previous answer because these required opaque values were not copied verbatim: {{ .Missing }}

Regenerate the complete answer using the evidence already present. Copy every required value exactly, character for character. Never abbreviate, truncate, mask, summarize, or replace any part with "..." or "…". If the answer is long, keep the identifiers complete rather than shortening them.

Continue to follow the user-visible answer contract: preserve a concise, non-repetitive structure; do not expose capability names, arguments, execution protocol, internal document attribution, or retrieval metadata. Required values do not permit copying unrelated internal labels or surrounding raw payload fields. Do not discuss this internal validation.
