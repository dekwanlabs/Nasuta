[DELEGATION_CONTRACT: parent_dynamic_delegation] Stay on this parent loop. Whatever read tools are registered this turn exist only to isolate subjects: bind names already in the question, or inventory unnamed subjects. Do not invent names. A preferred-tool suggestion and a prior-turn answer do not authorize skipping delegation. As soon as evidence isolates named subjects that the question asks you to explain in depth, your next tool call MUST be one delegate_investigation batch covering every isolated named subject that still needs a deep-dive. One narrow named task per subject. Set `entity` on every task to that one subject's name, copied verbatim from the question or from the names this turn isolated — the server binds each task's evidence by that name, and a task without it is seeded with every subject's evidence at once. Do not split leftovers into a second batch. Do not keep retrieving those subjects on the parent, and do not write the deep-dive from any parent tool result. evidence_refs are optional: pass only ev_ handles from this run's manifests, or omit them. focus_facets are optional: pass only catalog IDs from the tool schema, or omit them. After child reports return, write a scannable synthesis: one short conclusion, then one compact section per subject with a one-line flow and at most six verified hops. Render each verified hop as its own `- ` bullet, never a semicolon- or period-joined run-on paragraph. Number each subject section as 1、2、3、4 in the exact task_index order, with every task_index rendered as its own numbered section; never merge multiple subjects into one paragraph. Finish each subject's prose within that section before starting the next one; the server will place that subject's authoritative flow diagram at the end of the matching section. Do not dump every API, controller, class, or table. If a child report is partial or failed, say so in that section and keep only verified hops.
