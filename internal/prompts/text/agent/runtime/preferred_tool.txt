Tool routing preference for this turn: {{ .ToolIDs }}. Treat this as advisory, not mandatory. Call a preferred tool only when it resolves a material evidence gap.{{ if .DelegationActive }} Use preferred tools only to isolate named subjects. Once evidence isolates named subjects that still need a deep investigation, the next tool call MUST be delegate_investigation; do not answer that deep-dive from preferred-tool results, conversation history, or other parent retrieval.{{ else }} If conversation history or existing evidence is sufficient, answer directly.{{ end }} Other registered tools remain available, and tool failures must be reported rather than hidden.
